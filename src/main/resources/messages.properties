# =============================================================================
# Validation Messages (English)
# Custom validation messages for Bean Validation
# =============================================================================

# -----------------------------------------------------------------------------
# General Validation Messages
# -----------------------------------------------------------------------------
validation.required={0} is required
validation.invalid={0} is invalid
validation.size={0} must be between {min} and {max} characters
validation.min={0} must be at least {value}
validation.max={0} must not exceed {value}

# -----------------------------------------------------------------------------
# Event Validation Messages
# -----------------------------------------------------------------------------
# Name validations
validation.event.name.required=Event name is required
validation.event.name.size=Event name must be between {min} and {max} characters
validation.event.name.pattern=Event name contains invalid characters

# Description validations
validation.event.description.size=Description must not exceed {max} characters

# Date validations
validation.event.date.required=Event date is required
validation.event.date.future=Event date must be in the future
validation.event.date.future.hours=Event date must be at least {minHoursAhead} hours in the future
validation.event.startDate.required=Start date is required
validation.event.endDate.required=End date is required
validation.date.range.invalid=End date must be after start date
validation.date.future=The date must be in the future

# Venue validations
validation.event.venueId.required=Venue ID is required
validation.event.venueId.positive=Venue ID must be a positive number
validation.event.venue.notFound=Venue with ID {0} not found

# Capacity validations
validation.event.capacity.required=Event capacity is required
validation.event.capacity.positive=Capacity must be greater than 0
validation.event.capacity.max=Capacity must not exceed {value} people
validation.capacity.exceeded=Event capacity cannot exceed venue capacity

# Price validations
validation.event.price.required=Price is required
validation.event.price.positive=Price must be greater than 0
validation.event.price.max=Price must not exceed {value}
validation.price.invalid=Price must be between {min} and {max}

# Category validations
validation.event.category.required=Category is required
validation.event.category.size=Category must be between {min} and {max} characters
validation.event.category.invalid=Invalid category. Valid categories: {validCategories}

# Status validations
validation.event.status.invalid=Invalid event status. Valid statuses: ACTIVE, CANCELLED, POSTPONED, COMPLETED, DRAFT, SOLD_OUT

# -----------------------------------------------------------------------------
# Venue Validation Messages
# -----------------------------------------------------------------------------
# Name validations
validation.venue.name.required=Venue name is required
validation.venue.name.size=Venue name must be between {min} and {max} characters
validation.venue.name.unique=A venue with this name already exists in this city

# Address validations
validation.venue.address.required=Address is required
validation.venue.address.size=Address must be between {min} and {max} characters

# City validations
validation.venue.city.required=City is required
validation.venue.city.size=City must be between {min} and {max} characters
validation.venue.city.pattern=City name contains invalid characters

# Country validations
validation.venue.country.required=Country is required
validation.venue.country.size=Country must be between {min} and {max} characters
validation.venue.country.pattern=Country name contains invalid characters

# Capacity validations
validation.venue.capacity.required=Venue capacity is required
validation.venue.capacity.positive=Capacity must be greater than 0
validation.venue.capacity.max=Capacity must not exceed {value} people

# -----------------------------------------------------------------------------
# Category Validation Messages
# -----------------------------------------------------------------------------
validation.category.name.required=Category name is required
validation.category.name.size=Category name must be between {min} and {max} characters
validation.category.name.unique=A category with this name already exists
validation.category.color.pattern=Color must be a valid hex code (e.g., #FF5733)

# -----------------------------------------------------------------------------
# ID Validation Messages
# -----------------------------------------------------------------------------
validation.id.positive=ID must be a positive number
validation.id.required=ID is required

# -----------------------------------------------------------------------------
# Cross-field Validation Messages
# -----------------------------------------------------------------------------
validation.dates.startBeforeEnd=Start date must be before end date
validation.capacity.eventExceedsVenue=Event capacity ({eventCapacity}) cannot exceed venue capacity ({venueCapacity})
validation.price.range=Price must be between {min} and {max}

# -----------------------------------------------------------------------------
# Business Rule Messages
# -----------------------------------------------------------------------------
validation.event.venueHasEvents=Cannot delete venue with scheduled events
validation.event.alreadyCancelled=Event is already cancelled
validation.event.pastDate=Cannot modify events with past dates
validation.venue.hasActiveEvents=Venue has active events and cannot be deleted
